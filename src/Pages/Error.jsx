import React from 'react';

const Error = () => {
    return (
        <div>
            <h1 style={{color: 'red'}}>You have accessed a page that does not exist</h1>
        </div>
    );
};

export default Error;